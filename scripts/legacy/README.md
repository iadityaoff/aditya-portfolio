# Legacy one-off scripts

Old one-off patch/debug scripts that edited `public/prototype.html` and the
homepage markup while it was being built. Nothing imports or runs them; they're
kept for reference. They use paths relative to the repo root, so run them from
there (e.g. `python3 scripts/legacy/patch_work.py`) if you ever need one again.
